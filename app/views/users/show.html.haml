.span9#current-tournaments-div
  -if current_user.location_invalid?
    %p.alert.alert-danger.strong
      Hey! We couldn't find your exact work location from LDAP. So please
      = link_to 'Update it now', location_change_users_path, :remote => true
  %h3 Current Tournaments
  = render :partial => "tournaments/upcomingtournaments"

  %h3 Past Tournaments
  = render :partial => "tournaments/pasttournaments" 
